[project]
name = "comfyui-flowforge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
flowforge = "flowforge.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
